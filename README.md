# Fast React Pizza

A pizza ordering SPA built with React 18 + Vite, using React Router v6 (data APIs), Redux Toolkit for state management, and Tailwind CSS v3 for styling. The app supports browsing a menu, adding items to a cart, placing orders with geolocation, and updating order priority.

